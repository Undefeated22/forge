node_modules
.env
uploads*.txt
cookies.txt

# curl cookie jars — these hold live session tokens; never commit them
demo*.txt
c.txt
cookies.txt
test.txt
rb.txt
*.cookies
uploads/
native/target/

# personal Claude Code permissions — machine-local, never committed
.claude/settings.local.json
